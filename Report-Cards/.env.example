# Optional. If set, /api/upload-photo and DELETE /api/photo require this exact value
# in an "x-api-key" request header. Leave blank for local/LAN-only use.
UPLOAD_API_KEY=

# Optional. Defaults to 3000.
PORT=3000
